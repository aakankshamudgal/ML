class Calculator:
        def add(a,b):
                return a+b
        def sub(a,b):
                if(a>b):
                        return a-b
                else:
                        return b-a
        def mul(a,b):
                return a*b
        def div(a,b):
                return a/b
        def mod(a,b):
                return a%b
        x=add(4,5)
        print(x)
        y=sub(6,7)
        print(y)
        z=mul(3,2)

